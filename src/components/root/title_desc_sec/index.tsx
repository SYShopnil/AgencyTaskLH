import { ITitleDescSection } from '@src/types/root';
import React from 'react';

export const TitleDescSection = ({ desc, title }: ITitleDescSection) => {
  return (
    <div>
      <div></div>
      <div></div>
    </div>
  );
};
